import 'package:flutter/material.dart';
import 'package:my_app/peli.dart';

class Secondpage extends StatelessWidget{
  const Secondpage({super.key});
  @override
  Widget build(BuildContext context){
    return Scaffold(
      body: Center(
        child: Text('second page'),
      ),
    );
  }
}